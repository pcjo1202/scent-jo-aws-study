# LESSONS

작업 중 실수·혼동·잘못된 가정의 기록. `/lesson` 스킬로만 추가한다. **append-only.**
같은 원인이 2회 반복되면 CLAUDE.md "어기기 쉬운 규칙" 또는 훅(hookify)으로 승격한다.

---

## 2026-08-26 문서 SQL 일괄 치환에서 가짜 컬럼과 누락 쿼리

- 증상: docs/05 도출 쿼리에 존재하지 않는 `finished` 컬럼을 썼고(스키마는 `finished_at`), 오답 목록의 중첩 쿼리는 치환에서 누락됨. 외부 시선 리뷰가 적발
- 원인: ① 스키마를 다시 열어 대조하지 않고 컬럼명을 기억으로 지어냄 ② 들여쓰기가 다른 두 번째 쿼리를 exact-match 치환이 못 잡았는데, replace_all(count=0)이라 "성공"으로 착각
- 교정: 문서 속 SQL·코드를 고칠 땐 같은 파일의 스키마 정의를 grep으로 재확인하고, 일괄 치환 후 대상 패턴의 잔존 여부를 역방향 grep으로 검증한다
- 관련: docs/05-database.md / 문서 리뷰 P1-6 반영 커밋 → **승격됨 (CLAUDE.md)** — 2026-08-27 항목과 같은 원인으로 2회째
- 원인 ①(기억으로 단정)은 2026-08-27 「Vercel 빌드 스킵」 항목에서 재발 → **승격됨 (CLAUDE.md 「동작·정의를 기억으로 단정하지 않는다」)**

## 2026-08-27 exit 0을 "검증 통과"로 읽어 두 번 헛짚음

- 증상: ① ESLint 규칙이 실제로 발화하는지 보려고 scratchpad(**프로젝트 밖**)에 probe.ts를 만들어 돌렸더니 exit 0 — 규칙이 도는 걸로 읽었으나 실은 eslint가 프로젝트 밖 파일을 대상에서 제외한 것이었다. 같은 파일을 레포 안에 두자 바로 error가 났다 ② `pnpm test`를 `tail -3`으로만 보고 exit 0을 확인해 DoD 통과로 판정했는데, 잘린 출력 위쪽에 매 실행 ESM/CJS 경고가 있었다 (코드 리뷰 P2로 적발)
- 원인: **"오류가 없음"을 "일을 했음"으로 읽었다.** exit code는 검증이 대상에 닿았는지를 말해주지 않는다 — 0건 실행과 0건 실패는 똑같이 exit 0이다. 출력을 잘라 본 것도 같은 뿌리다: 통과 여부만 확인하고 실행 내용은 안 봤다
- 교정: 검증은 exit code에 **실행 건수와 출력 전문**을 함께 본다. 린터·테스트 probe는 반드시 레포 안에서 돌린다. 0건이면 "통과"가 아니라 **"대상 없음"으로 보고**한다
- 관련: SJO-31 / `eslint.config.base.mjs` · `vitest.config.ts` / 2026-08-26 항목과 같은 원인 (2회째 → CLAUDE.md 승격)

## 2026-08-27 Vercel 빌드 스킵의 주체를 기억으로 단정해 두 번 헛짚음

- 증상: docs만 바꾼 커밋에서 web·api가 둘 다 빌드되는 것을 "스킵 미동작"으로 읽고 `vercel.json`에 ① `ignoreCommand: npx turbo-ignore`를 넣어 "붙였다"고 보고했다. 그 뒤 web 단독 커밋에서 api 프리뷰가 죽는 것을 발견하자 이번엔 ② `ignoreCommand: "exit 1"`로 "강제했다"고 보고했다. **①은 스킵을 켜려는, ②는 끄려는 정반대 시도였는데 둘 다 아무것도 바꾸지 못했다.** 실제 주체는 프로젝트 설정 **Skip deployment**(Settings → Build and Deployment → Root Directory)였다 — 그 설정을 끄자 바로 해결됐다. (`ignoreCommand`가 이 스킵을 되돌리지 못한다는 것까지는 실측했으나, 그 키가 무엇을 하는지는 확인하지 못했다 — 또 단정하지 않는다.) 그 사이 잘못된 결론("pnpm workspaces 규약만으로는 스킵되지 않는다")을 `docs/03`에 커밋했다가 되돌리는 데 커밋 3개를 더 썼다
- 원인: **관측 하나를 가설에 유리하게 읽고, 원문을 열기 전에 수정부터 적용했다.** 첫 관측(docs만 바꾼 커밋에서 web·api가 둘 다 빌드됨)을 "스킵 미동작"으로 해석했는데, 실제로는 루트 파일이 어느 패키지 소유도 아니라 둘 다 영향 있음으로 판정된 **정상 동작**이었다. 그 다음 관측(web 단독 커밋에서 api 스킵)이 우연히 가설과 맞아떨어져 확증편향이 굳었다. 반례를 찾는 대신 수정안을 시도했고, 실패할 때마다 원인을 재검토하지 않고 같은 가설 위에서 변형만 했다
- 교정: 외부 도구·플랫폼의 동작은 **원문을 열어 확인한 뒤** 고친다. 관측 몇 건으로 세운 가설은 수정을 적용하기 전에 **반례를 먼저 찾는다**. 특히 문서가 명세인 이 레포에서, 검증되지 않은 결론을 `docs/`에 커밋하지 않는다 — 되돌리는 비용이 원문 확인보다 크다
- 관련: SJO-3 / `docs/03-architecture.md` §빌드 스킵 · `apps/api/vercel.json` / 원문: Vercel 「Configuring a Build」 → Root Directory → *Skipping unaffected projects* / 2026-08-26 항목의 원인 ①과 같은 원인 (2회째 → CLAUDE.md 승격)

## 2026-08-27 닫은 이슈에 두 번째 브랜치를 파서 Done이 조용히 풀렸다

- 증상: SJO-31이 05:04 `/done`으로 Done이 됐는데 05:09 `In Progress`로 되돌아가 약 4시간 열린 채 남았다. 그 사이 SJO-2·SJO-3·SJO-33은 정상적으로 닫혀서, 로드맵상 E0만 안 끝난 것처럼 보였다. 다음 세션이 `/start SJO-31`로 착수하려다 발견했다 — 아무도 실패를 보지 못했고 오류도 나지 않았다
- 원인: 같은 이슈 ID로 두 번째 브랜치(`docs/sjo-31-lesson`)를 팠다. Linear↔GitHub 연동은 브랜치명의 `sjo-31`로 이슈를 찾아 **PR이 열리는 순간** In Progress로 되돌린다 — PR #2 열림 `05:09:39Z` → 상태 변경 `05:09:42Z`(3초 후)로 확증했다. 머지(`05:09:48Z`)는 Done으로 복귀시키지 않았는데, 이 레포가 `/done`의 증거 게이트를 지키려고 의도적으로 `Fixes`·`Closes` 대신 `Ref SJO-N`을 쓰기 때문이다. **여는 자동화는 켜져 있고 닫는 자동화는 꺼둔 비대칭** — 닫힌 이슈에 브랜치를 하나 더 파면 되돌릴 주체가 없다. `CLAUDE.md` 「브랜치와 PR」에 「이슈 하나 = 브랜치 하나 = PR 하나」가 이미 있었으나, 어겼을 때의 대가가 적혀 있지 않아 규칙이 지켜질 이유가 약했다
- 교정: 이슈를 닫은 뒤 나오는 후속 작업(lesson·문서·리뷰 반영)은 **새 이슈를 만든다.** SJO-33이 그렇게 했고 PR #6은 아무 문제를 일으키지 않았다 — 같은 날, 같은 종류의 작업이라 대조군이 명확하다. 닫힌 이슈 ID로는 브랜치를 파지 않는다. 이미 팠다면 머지 후 이슈 상태를 눈으로 확인한다 (`Ref`는 자동으로 닫지 않는다)
- 관련: SJO-34 / SJO-31 · PR #2(문제) · SJO-33 · PR #6(정상 대조군) / `CLAUDE.md` 「브랜치와 PR」 — 1회째, 승격은 2회째에 그 줄로

## 2026-08-28 프로브를 한 형태만 만들어 린트 미탐을 못 봤다

- 증상: Tailwind 임의값을 막는 `no-restricted-syntax` 규칙을 넣고, `className="p-[13px] bg-[#abc]"` 샘플로 **실제로 1건 잡히는 것을 확인한 뒤** `docs/10-conventions.md`와 `apps/web/CLAUDE.md`에 "린트가 막는다"고 단언해 커밋했다. 실제로는 5개 작성 형태 중 1개만 잡았다 — `className={'…'}`·템플릿 리터럴·`cn('…')`·삼항 분기가 전부 통과했고, 그쪽이 상태 분기가 있는 컴포넌트에서는 오히려 주 경로다. code-reviewer와 design-reviewer가 각자 프로브 파일을 만들어 같은 미탐을 독립적으로 잡아냈다
- 원인: **"규칙이 도는가"를 확인하고 "규칙이 무엇을 놓치는가"는 확인하지 않았다.** 2026-08-27 항목의 교정("위반 샘플을 넣어 실제로 잡히는지 본다")을 그대로 따랐는데, 그 샘플이 한 형태뿐이었다. 원인은 셀렉터의 직계 자식 결합자(`>`)였고 자손 결합자면 잡혔다 — 즉 **놓친 경로가 어렵거나 드물어서가 아니라, 놓칠 수 있다는 것을 아예 세어보지 않아서**다. 강제 장치는 통과 케이스가 아니라 우회 경로로 평가해야 하는데 통과 케이스만 봤다
- 교정: 강제 장치(린트 규칙·훅·검증 스크립트)를 추가할 때는 **우회 경로를 먼저 열거하고 그 전부를 프로브 파일 하나에 담아** 한 번에 돌린다. "N개 중 M개 검출"을 세지 않으면 검증한 것이 아니다. 통과해야 할 정상 케이스도 같은 파일에 넣어 오탐까지 함께 본다 — 이번에도 그 덕에 임의 variant(`data-[state=open]:`)를 오탐하지 않게 `(?!:)`를 넣을 수 있었다
- 관련: SJO-17 / `apps/web/eslint.config.mjs` · `docs/10-conventions.md` 「스타일 저작」 / 2026-08-27 「exit 0을 검증 통과로 읽어…」와 같은 계열 (2회째) → 승격됨 (CLAUDE.md 「어기기 쉬운 규칙」)

## 2026-08-28 `gh pr merge --delete-branch`가 워크트리에서 실패하는데 머지는 이미 성공해 있다

- 증상: `/done` 6번에서 `gh pr merge 11 --merge --delete-branch`가 `failed to run git: fatal: 'main' is already used by worktree at '…/orca/projects/aws-study'`로 죽었다. 종료 코드만 보면 머지가 안 된 것처럼 보이지만, `gh pr view --json state`는 `MERGED` + merge commit `d8f7431`을 돌려줬다 — **원격 머지는 이미 끝났고 gh가 그 뒤에 하는 로컬 `main` 체크아웃에서만 죽은 것**이다. 그대로 재시도했으면 "이미 머지됨" 오류를 보고 원인을 다시 헷갈렸을 것이다
- 원인: `gh pr merge`는 머지 후 로컬 브랜치를 정리하려고 `main`을 체크아웃한다. Orca 워크트리에서는 `main`이 **다른 워크트리에 이미 체크아웃돼 있어** git이 거부한다. 실패 메시지가 머지가 아니라 정리 단계의 것인데, 한 명령의 exit code가 두 단계를 덮고 있어 구분되지 않는다. `CLAUDE.md`가 워크트리 세션을 표준 흐름으로 정한 뒤에는 **매번** 걸린다
- 교정: 워크트리에서는 `--delete-branch`를 붙이지 않는다. 머지 후 `gh pr view <N> --json state,mergeCommit`으로 결과를 확인하고, 브랜치는 `git push origin --delete <branch>` + `git fetch origin --prune`으로 정리한다. 로컬 `main` pull은 이 워크트리에서 하지 않는다 — 메인 워크트리 소관이다. **명령 하나가 여러 단계를 품으면 실패 메시지가 어느 단계 것인지부터 확인한다**
- 관련: SJO-17 / PR #11 / `.claude/skills/done/SKILL.md` 6번 · `CLAUDE.md` 「그 세션은 별도 워크트리에서 연다」 — 1회째, 승격은 2회째에

## 2026-08-28 정제 전 텍스트로 집계해 「섹션 결손 20건」을 사실로 보고

- 증상: SJO-4 착수 계획 코멘트에 "요구사항/조건 1017 · 정답 해설 1018 · 오답 해설 **999**, 결손이 있으니 파서가 전제로 깔면 안 된다"를 표로 확정해 적었다. **1019문항 모두 세 절을 갖고 있었다.** 파서를 만들고 실제 모듈로 재집계하고 나서야 발견해 코멘트를 정정했다
- 원인: `pdftotext` 출력의 `\f`(formfeed)를 **제거하기 전** 텍스트에 `grep -cE "^ *오답 해설"`를 돌렸다. 페이지 첫 줄은 `\f   오답 해설`이 되어 `^ *` 에 걸리지 않는다. 결손처럼 보인 20건은 전부 페이지 첫 줄에 온 절 제목이었다. **숫자가 그럴듯한 것이 함정이었다** — 1019 중 999는 "20개쯤 예외가 있구나"로 읽히지 0으로 보이지 않는다. 0이었다면 도구를 의심했을 것이다. 정제 파이프라인을 이미 코드로 갖고 있으면서 그걸 안 쓰고 원본에 애드혹 grep을 돌린 것이 실수의 형태다
- 교정: 파이프라인이 **정제한 뒤** 무엇이 남는지가 사실이면, 집계도 **그 정제를 통과한 데이터**에서 낸다 — 이미 만든 모듈로 재확인한다. 애드혹 grep의 결과는 가설이지 사실이 아니다. 특히 전체 대비 **그럴듯하게 조금 모자란 숫자**는 0건보다 위험하다. 보고에 숫자를 적기 전에 그 숫자를 만든 도구가 대상 전부에 닿았는지 본다
- 관련: SJO-4 / `scripts/src/questions/split-blocks.ts` · 계획 코멘트 정정 / 2026-08-27 「exit 0」·「Vercel 빌드 스킵」과 같은 뿌리 — **측정이 대상에 닿았는지 확인하지 않음** (3회째) → **승격됨 (CLAUDE.md 「exit 0」 규칙에 부분 집계 절 추가)**

## 2026-08-28 계산기를 검증하지 않고 돌려 물리적으로 불가능한 수치를 발행

- 증상: SJO-38에서 적록색약 ΔE를 계산해 `DESIGN.md`·PR 본문·이슈 코멘트 **세 곳에 발행**했는데 값이 틀렸다. `correct`×`error` Dark가 정상 시각 55.8인데 색각 시뮬레이션 값이 **58.0** — 적록 쌍은 시뮬레이션에서 반드시 수축하므로 정상보다 클 수 없다. code-reviewer와 design-reviewer가 **각자 독립적으로 재계산해서야** 잡혔다. 실제 값은 9.9~10.5였다
- 원인: 두 가지가 겹쳤다. ① **계산기를 검증하지 않았다.** Viénot 행렬은 선형 RGB에서 정의되는데 감마 인코딩된 값에 적용했다. 같은 세션에서 **대비 계산기는 기지값 5건을 재현시켜 검증해놓고**(`#6750A4`+흰=6.44 등), 색각 계산기에는 같은 잣대를 안 댔다 — 검증할 "알려진 정답"이 손에 없다는 이유로 그냥 믿었다. 불변식(`시뮬레이션 ΔE ≤ 정상 ΔE`)은 정답표 없이도 세울 수 있었는데 세우지 않았다. ② **조사 범위를 이슈 본문이 지목한 쌍에 맞췄다.** 본문이 "`primary`×`error`를 확인하라"고 해서 그 쌍만 봤고, 유채 역할 7개 전 조합 42쌍을 스캔하자 **더 위험한 쌍이 나왔다** — `secondary-container`×`correct-container`(Dark 2.2, JND 2.3 미만). 채점 화면에서 "내가 고른 선택지"와 "정답 선택지"가 나란히 놓이는 쌍이라 지목된 쌍보다 치명적이다
- 교정: **수치를 발행하기 전에 그 수치를 만든 계산기를 먼저 검증한다.** 기지값이 있으면 재현시키고, 없으면 **불변식을 세워 위생 검사로 건다** — "시뮬레이션은 원본보다 차이를 키울 수 없다" 같은 것은 정답표 없이도 성립한다. 검증한 계산기와 안 한 계산기를 한 세션에서 섞어 쓰지 않는다. 그리고 **이슈 본문이 지목한 위험은 가설이지 범위가 아니다** — 지목된 쌍을 확인하되 같은 종류의 쌍을 전수로 훑어 지목이 맞았는지부터 본다. 이번엔 지목이 틀렸다
- 관련: SJO-40 / SJO-38 · `DESIGN.md` 「색으로만 갈리지 않는 쌍」 · 후속 SJO-39(검증 스크립트) / 2026-08-27 「exit 0」 · 2026-08-28 「부분 집계」 · 2026-08-28 「프로브를 한 형태만」과 같은 뿌리 — **검증 수단 자체를 검증하지 않음** (4회째) → 승격됨 (CLAUDE.md 「어기기 쉬운 규칙」)

## 2026-09-03 산문만 고치고 그것을 요약·인용한 곳을 남겨 표와 본문이 반대를 말했다

- 증상: `DESIGN.md`·`docs/02`를 화면 명세로 승격하면서, 한 규칙을 고칠 때마다 **그 규칙을 요약하거나 인용한 다른 자리**가 옛 규칙을 그대로 말한 채 남았다. 하단 액션 바에서 배경을 걷어냈는데 「surface container를 화면에 매핑」 표는 여전히 `하단 액션 바 | surface-container`였고, 라벨을 `sr-only`로 내렸는데 「색 단독 전달 금지」 표는 여전히 「정답」·「내 선택」 텍스트를 요구했고, 소제목을 「라우트가 정하는 고정 링크다」로 바꿨는데 다른 절이 옛 제목을 `「」`로 인용했다. **같은 형태를 한 세션에서 6번 만들었다** — 두 문서만 준 서브에이전트 검증 8회가 매번 새로 잡아냈고, 마지막 2회는 코드 리뷰 반영이 만든 것이었다
- 원인: **고친 규칙의 파급을 애드혹으로 훑었다.** 눈에 띄는 곳만 고치니 매번 남았다. 남는 자리는 종류가 정해져 있다 — ① 요약 **표**의 셀 ② 하드코딩된 **상수**(「일곱 개」·「64px」·「정답 2개」) ③ **절대 표현**(「~뿐이다」·「항상」·「전부」·「하나다」) ④ `「」`로 인용한 **소제목** ⑤ "~이므로"로 시작해 **폐기된 전제를 근거로 든 문장**. ⑤가 가장 나빴다: 「깊이는 1이다」가 거짓이 된 뒤에도 그게 고정 뒤로가기의 근거로 남아, 근거가 거짓인 채 결론만 맞는 문장이 됐다. 더 나쁜 것은 **2차 검증에서 그걸 고치려다 992px 세 번째 경계를 새로 만들어 같은 실패를 한 겹 더 쌓은 것**이다(4차에서 철회). 공백은 "모른다"로 읽히지만 **표와 산문의 충돌은 둘 다 정본으로 읽혀** 구현자가 어느 쪽을 어길지 모른다
- 교정: 규칙을 고치면 **그 규칙을 참조하는 곳을 전수로 뽑아** 대조한다 — 애드혹 grep이 아니라 기계적으로. 마지막에 `「」` 인용을 전부 추출해 실제 절 제목과 대조하니 남은 1건이 한 번에 나왔다. 고친 규칙마다 위 5종을 검색어로 만든다: 바뀐 **값**(64→88이면 `64`), 바뀐 **역할명**, 바뀐 **소제목 전문**, 그리고 그 절을 가리키는 **참조 표현**. 개수를 세지 않으면 고친 것이 아니다
- 관련: SJO-47 / SJO-44 · `DESIGN.md` · `docs/02-features.md` / 새 원인 계열 — **개정의 파급을 전수로 훑지 않음**. 세션 내 6회 반복이라 1회로 세지 않는다 → 승격됨 (CLAUDE.md 「어기기 쉬운 규칙」)

## 2026-09-03 검증 프롬프트의 범위를 이슈가 지목한 것에 맞춰 그 밖을 못 봤다

- 증상: DoD가 「두 문서만 읽고 4개 화면을 구현할 수 있다」라 서브에이전트에 **8회** 물어 BLOCKER 0까지 끌고 갔다. 그 뒤 코드 리뷰가 **8회가 전부 놓친 것**을 잡았다 — 상시 네비게이션을 없애면서 대시보드에 `/exam`·`/notes`·`/anatomy` 진입점을 안 만들어, 「새 모의고사 시작」이 `/exam`에만 있으므로 **모의고사를 시작할 방법이 없는 앱**이 된다
- 원인: **내가 만든 검증 프롬프트가 이슈의 범위(4화면)를 그대로 물려받았다.** 허브-스포크는 그 4화면 안에서는 완결이라 8회 모두 통과했고, 9화면 전체에서만 셋이 고아가 된다. 리뷰어는 diff 전체를 봤기 때문에 봤다. 더 뼈아픈 것은 **근거 문장이 스스로를 반증하고 있었다**는 점이다 — 허브-스포크의 근거로 "최상위 진입점이 6개(`/`·`/study`·`/review`·`/exam`·`/notes`·`/anatomy`)"라고 여섯을 열거해 놓고, 바로 다음 문장에서 대시보드가 이미 허브인 증거로 셋만 들었다. 빠진 셋이 같은 문단에 적혀 있었는데 못 봤다
- 교정: 검증을 시킬 때 **범위를 이슈에서 베끼지 않는다.** 이슈가 지정한 대상은 산출물의 범위이지 **영향의 범위**가 아니다. 구조를 바꾸는 결정(네비게이션·라우팅·공통 골격)은 그 구조를 물려받는 **전체 집합**에 대해 묻는다 — 4화면을 그렸어도 9화면 전부에 대해 「도달 가능한가」를 묻는다. 그리고 **근거로 열거한 목록은 결론과 대조한다**: N개를 열거했으면 결론이 N개를 다 덮는지 센다
- 관련: SJO-47 / SJO-44 · `DESIGN.md` 「네비게이션은 허브-스포크다」 · `docs/02` 「화면 구성 요소」 / 2026-08-28 SJO-40 「이슈 본문이 지목한 위험은 가설이지 조사 범위가 아니다」와 같은 뿌리 (5회째) — 그 규칙은 이미 CLAUDE.md에 있으나 **자기가 만든 검증 프롬프트**에도 걸린다는 것이 새로 드러났다


## 2026-09-04 검증 환경이 프로덕션보다 관대해 「통과」가 두 번 거짓으로 나왔다

- 증상: 한 세션에서 **같은 형태의 거짓 통과를 두 번** 만났다. ① 이슈가 지시한 `node dist/main.js`가 **수정 전 코드에서 부팅 성공 · `/health` 200**을 줬다 — 프로덕션은 같은 코드로 `ERR_REQUIRE_ESM`이었다. ② web 프리렌더 수정을 **원격 api(500)** 대상 빌드로 재서 「통과」를 받고 커밋·PR까지 올렸는데, 프리뷰 배포가 같은 자리에서 죽었다. 빠른 로컬 스텁으로 같은 500을 주니 **수정 전과 똑같이** 죽었다 — 고친 것이 아무것도 없었다
- 원인: **검증 환경이 실패를 가리는 방향으로 프로덕션과 달랐고, 그 차이를 재기 전에 「통과」를 근거로 삼았다.** ①은 런타임 차이다 — 로컬 Node 24는 `require(esm)`를 구현하지만 Vercel Fluid의 자체 모듈 로더는 구현하지 않는다. 같은 `nodeVersion: 24.x`라 버전만 맞춰 보고 동등하다고 여겼다. ②는 **타이밍**이다 — 원격 5xx는 왕복이 느려서 Next가 정적 셸을 확정한 **뒤에** 거절이 도착해 우연히 통과한다. 둘 다 「통과」가 수정이 아니라 **환경의 관대함**이 만든 것인데, 통과를 받은 순간 검증을 멈췄다. ②는 특히 나빴다 — 음성 대조(수정 전 실패)를 **원격으로만** 확인해 놓고 그것을 하네스가 유효하다는 증거로 썼다. 실제로는 수정 전·후가 원격에서 갈린 것도 우연이었다
- 교정: **고쳤다는 근거는 「프로덕션과 같은 조건에서 실패하던 것이 이제 통과한다」여야 한다.** 하네스를 믿기 전에 하네스를 검증한다 — **수정 전 코드가 그 하네스에서 반드시 실패해야 하고, 그 실패가 프로덕션의 실패와 같은 종류여야 한다.** ①은 `--no-experimental-require-module`로 로더의 엄격함을 맞춰 해결했다. ②는 **느린 원격 대신 빠른 로컬 스텁**으로 재야 했다 — 네트워크 지연은 경합을 덮는다. 조건이 하나면 부족하다: 정상·빠른 5xx·2xx인데 본문이 JSON이 아닌 경우(별칭 오배치) **세 조건 × 후보들의 매트릭스**로 재니 후보 3개 중 1개만 3/3이었고, 「통과」 1개만 봤으면 또 틀렸다
- 관련: SJO-49 / `apps/api/smoke.mjs` · `apps/web/app/page.tsx` · `.claude/rules/web-state.md` · `docs/03` 「알려진 리스크」 · `docs/06` 「검증」 / 새 원인 계열 — **검증 환경이 프로덕션보다 관대함**. 한 세션에서 2회라 승격 대상 → 승격됨 (CLAUDE.md 「어기기 쉬운 규칙」)
