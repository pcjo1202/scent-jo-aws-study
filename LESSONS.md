# LESSONS

작업 중 실수·혼동·잘못된 가정의 기록. `/lesson` 스킬로만 추가한다. **append-only.**
같은 원인이 2회 반복되면 CLAUDE.md "어기기 쉬운 규칙" 또는 훅(hookify)으로 승격한다.

---

## 2026-08-26 문서 SQL 일괄 치환에서 가짜 컬럼과 누락 쿼리

- 증상: docs/05 도출 쿼리에 존재하지 않는 `finished` 컬럼을 썼고(스키마는 `finished_at`), 오답 목록의 중첩 쿼리는 치환에서 누락됨. 외부 시선 리뷰가 적발
- 원인: ① 스키마를 다시 열어 대조하지 않고 컬럼명을 기억으로 지어냄 ② 들여쓰기가 다른 두 번째 쿼리를 exact-match 치환이 못 잡았는데, replace_all(count=0)이라 "성공"으로 착각
- 교정: 문서 속 SQL·코드를 고칠 땐 같은 파일의 스키마 정의를 grep으로 재확인하고, 일괄 치환 후 대상 패턴의 잔존 여부를 역방향 grep으로 검증한다
- 관련: docs/05-database.md / 문서 리뷰 P1-6 반영 커밋 → **승격됨 (CLAUDE.md)** — 2026-08-27 항목과 같은 원인으로 2회째
- 원인 ①(기억으로 단정)은 2026-08-27 「Vercel 빌드 스킵」 항목에서 재발 → **승격됨 (CLAUDE.md 「동작·정의를 기억으로 단정하지 않는다」)**

## 2026-08-27 exit 0을 "검증 통과"로 읽어 두 번 헛짚음

- 증상: ① ESLint 규칙이 실제로 발화하는지 보려고 scratchpad(**프로젝트 밖**)에 probe.ts를 만들어 돌렸더니 exit 0 — 규칙이 도는 걸로 읽었으나 실은 eslint가 프로젝트 밖 파일을 대상에서 제외한 것이었다. 같은 파일을 레포 안에 두자 바로 error가 났다 ② `pnpm test`를 `tail -3`으로만 보고 exit 0을 확인해 DoD 통과로 판정했는데, 잘린 출력 위쪽에 매 실행 ESM/CJS 경고가 있었다 (코드 리뷰 P2로 적발)
- 원인: **"오류가 없음"을 "일을 했음"으로 읽었다.** exit code는 검증이 대상에 닿았는지를 말해주지 않는다 — 0건 실행과 0건 실패는 똑같이 exit 0이다. 출력을 잘라 본 것도 같은 뿌리다: 통과 여부만 확인하고 실행 내용은 안 봤다
- 교정: 검증은 exit code에 **실행 건수와 출력 전문**을 함께 본다. 린터·테스트 probe는 반드시 레포 안에서 돌린다. 0건이면 "통과"가 아니라 **"대상 없음"으로 보고**한다
- 관련: SJO-31 / `eslint.config.base.mjs` · `vitest.config.ts` / 2026-08-26 항목과 같은 원인 (2회째 → CLAUDE.md 승격)

## 2026-08-27 Vercel 빌드 스킵의 주체를 기억으로 단정해 두 번 헛짚음

- 증상: docs만 바꾼 커밋에서 web·api가 둘 다 빌드되는 것을 "스킵 미동작"으로 읽고 `vercel.json`에 ① `ignoreCommand: npx turbo-ignore`를 넣어 "붙였다"고 보고했다. 그 뒤 web 단독 커밋에서 api 프리뷰가 죽는 것을 발견하자 이번엔 ② `ignoreCommand: "exit 1"`로 "강제했다"고 보고했다. **①은 스킵을 켜려는, ②는 끄려는 정반대 시도였는데 둘 다 아무것도 바꾸지 못했다.** 실제 주체는 프로젝트 설정 **Skip deployment**(Settings → Build and Deployment → Root Directory)였다 — 그 설정을 끄자 바로 해결됐다. (`ignoreCommand`가 이 스킵을 되돌리지 못한다는 것까지는 실측했으나, 그 키가 무엇을 하는지는 확인하지 못했다 — 또 단정하지 않는다.) 그 사이 잘못된 결론("pnpm workspaces 규약만으로는 스킵되지 않는다")을 `docs/03`에 커밋했다가 되돌리는 데 커밋 3개를 더 썼다
- 원인: **관측 하나를 가설에 유리하게 읽고, 원문을 열기 전에 수정부터 적용했다.** 첫 관측(docs만 바꾼 커밋에서 web·api가 둘 다 빌드됨)을 "스킵 미동작"으로 해석했는데, 실제로는 루트 파일이 어느 패키지 소유도 아니라 둘 다 영향 있음으로 판정된 **정상 동작**이었다. 그 다음 관측(web 단독 커밋에서 api 스킵)이 우연히 가설과 맞아떨어져 확증편향이 굳었다. 반례를 찾는 대신 수정안을 시도했고, 실패할 때마다 원인을 재검토하지 않고 같은 가설 위에서 변형만 했다
- 교정: 외부 도구·플랫폼의 동작은 **원문을 열어 확인한 뒤** 고친다. 관측 몇 건으로 세운 가설은 수정을 적용하기 전에 **반례를 먼저 찾는다**. 특히 문서가 명세인 이 레포에서, 검증되지 않은 결론을 `docs/`에 커밋하지 않는다 — 되돌리는 비용이 원문 확인보다 크다
- 관련: SJO-3 / `docs/03-architecture.md` §빌드 스킵 · `apps/api/vercel.json` / 원문: Vercel 「Configuring a Build」 → Root Directory → *Skipping unaffected projects* / 2026-08-26 항목의 원인 ①과 같은 원인 (2회째 → CLAUDE.md 승격)

## 2026-08-27 닫은 이슈에 두 번째 브랜치를 파서 Done이 조용히 풀렸다

- 증상: SJO-31이 05:04 `/done`으로 Done이 됐는데 05:09 `In Progress`로 되돌아가 약 4시간 열린 채 남았다. 그 사이 SJO-2·SJO-3·SJO-33은 정상적으로 닫혀서, 로드맵상 E0만 안 끝난 것처럼 보였다. 다음 세션이 `/start SJO-31`로 착수하려다 발견했다 — 아무도 실패를 보지 못했고 오류도 나지 않았다
- 원인: 같은 이슈 ID로 두 번째 브랜치(`docs/sjo-31-lesson`)를 팠다. Linear↔GitHub 연동은 브랜치명의 `sjo-31`로 이슈를 찾아 **PR이 열리는 순간** In Progress로 되돌린다 — PR #2 열림 `05:09:39Z` → 상태 변경 `05:09:42Z`(3초 후)로 확증했다. 머지(`05:09:48Z`)는 Done으로 복귀시키지 않았는데, 이 레포가 `/done`의 증거 게이트를 지키려고 의도적으로 `Fixes`·`Closes` 대신 `Ref SJO-N`을 쓰기 때문이다. **여는 자동화는 켜져 있고 닫는 자동화는 꺼둔 비대칭** — 닫힌 이슈에 브랜치를 하나 더 파면 되돌릴 주체가 없다. `CLAUDE.md` 「브랜치와 PR」에 「이슈 하나 = 브랜치 하나 = PR 하나」가 이미 있었으나, 어겼을 때의 대가가 적혀 있지 않아 규칙이 지켜질 이유가 약했다
- 교정: 이슈를 닫은 뒤 나오는 후속 작업(lesson·문서·리뷰 반영)은 **새 이슈를 만든다.** SJO-33이 그렇게 했고 PR #6은 아무 문제를 일으키지 않았다 — 같은 날, 같은 종류의 작업이라 대조군이 명확하다. 닫힌 이슈 ID로는 브랜치를 파지 않는다. 이미 팠다면 머지 후 이슈 상태를 눈으로 확인한다 (`Ref`는 자동으로 닫지 않는다)
- 관련: SJO-34 / SJO-31 · PR #2(문제) · SJO-33 · PR #6(정상 대조군) / `CLAUDE.md` 「브랜치와 PR」 — 1회째, 승격은 2회째에 그 줄로
