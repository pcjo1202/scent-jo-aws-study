/**
 * 앱 이름. 문서 제목(`app/layout.tsx`의 `metadata`)과 허브 앱바 제목이 같은 값이고, 화면
 * 이름이 아니라 **앱의 이름**이라 화면 슬라이스가 아니라 여기 있다
 * (`.claude/rules/code-conventions.md` 「SSOT」).
 */
export const APP_TITLE = 'AWS SAA-C03 학습'
