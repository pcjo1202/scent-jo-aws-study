# 08. 디자인 시스템

브랜드 아이덴티티 문서가 아니다. **가독성 명세**다.

이 앱의 화면은 텍스트 밀도가 극단적이다. 긴 한국어 지문 + 선택지 5개 + 정답 해설 + 선택지별 오답 해설. 한 문항이 화면 3~4개 분량이고, 그걸 **1019번 본다.** 행간 하나가 학습 지속 시간을 좌우한다.

## 원칙

1. **본문 가독성이 다른 모든 것을 이긴다.** 예뻐 보이려고 글자를 줄이지 않는다
2. **색에 의미를 최소한만 싣는다.** 색을 많이 쓸수록 각 색의 의미가 약해진다
3. **정보를 색 단독으로 전달하지 않는다.** 아이콘·텍스트를 병기한다
4. **모션은 방해다.** 1019번 반복할 화면에 애니메이션을 넣지 않는다

## 타이포그래피

### 폰트

```css
font-family: system-ui, -apple-system, "Apple SD Gothic Neo",
             "Noto Sans KR", "Malgun Gothic", sans-serif;
```

시스템 폰트만 쓴다. 네트워크 비용이 0이고 첫 렌더가 즉시다. Apple에서는 SF Pro + Apple SD Gothic Neo, Android/Chrome에서는 Roboto + Noto Sans KR로 붙는다. 한국어 본문 품질이 충분하다.

> Pretendard를 자체 CDN에 올려 쓰는 선택지가 있다. **v1에서는 하지 않는다.** 시스템 폰트로 읽어본 뒤 실제로 불편하면 그때 넣는다.

### 스케일

| 역할 | 크기 | 행간 | 용도 |
|---|---|---|---|
| `display` | 24px | 1.4 | 화면 제목 |
| `heading` | 20px | 1.5 | 섹션 제목, 해부서 절 제목 |
| `body` | **16px** | **1.75** | **지문, 선택지, 해설** |
| `small` | 14px | 1.6 | 메타 정보, 칩, 라벨 |
| `caption` | 13px | 1.5 | 진행 표시, 타임스탬프 |

**본문 행간 1.75는 협상 대상이 아니다.** 한글은 라틴 문자보다 글자 밀도가 높아 같은 행간에서 훨씬 답답하다. 긴 지문에서 1.5는 벽처럼 보인다.

**모바일에서도 본문을 16px로 유지한다.** 14px로 줄이면 지문 읽기가 고통스러워지고, iOS에서 입력 요소가 16px 미만이면 자동 확대가 일어난다.

### 읽기 칼럼

```css
max-width: 42rem;   /* 672px ≈ 한글 42자 */
margin-inline: auto;
padding-inline: 1rem;   /* 모바일 */
```

한 줄이 길어지면 다음 줄 시작점을 놓친다. 넓은 화면에서도 본문은 이 폭을 넘지 않는다.

## 색

중립 계열 + 액센트 1개 + 의미색 3개. 그 이상 늘리지 않는다.

### 토큰

| 토큰 | Light | Dark | 용도 |
|---|---|---|---|
| `bg` | `#ffffff` | `#0f1012` | 페이지 배경 |
| `surface` | `#f7f8f9` | `#17181b` | 해설 블록, 카드 |
| `border` | `#e5e7eb` | `#27282c` | 구분선, 선택지 테두리 |
| `text` | `#18181b` | `#e8e8ea` | 본문 |
| `text-muted` | `#71717a` | `#8e8e93` | 메타, 보조 |
| `accent` | `#2563eb` | `#60a5fa` | 선택 상태, 링크 |
| `correct` | `#16a34a` | `#4ade80` | 정답 |
| `wrong` | `#dc2626` | `#f87171` | 오답 |

다크 배경에 순수 검정(`#000`)을 쓰지 않는다. 대비가 과해 긴 텍스트에서 눈이 빨리 피로해진다.

본문 대비는 라이트·다크 모두 4.5:1 이상을 지킨다.

### 색 사용 규칙

- **`accent`는 "선택됨"과 링크에만.** 버튼·헤더·강조에 남발하면 선택 상태가 안 보인다
- **`correct` / `wrong`은 채점 결과에만.** 다른 곳에 초록·빨강을 쓰지 않는다
- **카테고리 11개에 색을 배정하지 않는다**

### 카테고리에 색을 쓰지 않는 이유

11색 팔레트는 예외 없이 실패한다. 서로 구분되지 않고, 다크모드에서 무너지고, 색각 이상에서 절반이 겹친다. 무엇보다 **카테고리 색은 정오 색과 경쟁한다.**

카테고리는 중립색 라벨 칩으로만 표시한다.

**예외 하나**: 대시보드의 카테고리별 정답률 막대는 정답률 값에 따라 `wrong`↔`correct` 사이를 간다. 여기서는 색이 카테고리가 아니라 **값**을 나타내므로 의미가 충돌하지 않는다.

비교노트의 ★ 중요도도 색을 쓰지 않는다. ★ 문자 그대로 1~3개를 보여준다.

### 다크모드

`prefers-color-scheme`을 기본으로 하고 수동 토글을 둔다. 상태는 `system` / `light` / `dark` 3가지이며 `localStorage`에 저장한다.

밤에 폰으로 공부하는 시나리오가 실재하므로 선택 기능이 아니다.

## 간격

4px 기반. `4 · 8 · 12 · 16 · 24 · 32 · 48`

| 위치 | 값 |
|---|---|
| 지문 ↔ 선택지 | 24 |
| 선택지 사이 | 8 |
| 선택지 내부 패딩 | 12 / 16 |
| 해설 블록 사이 | 16 |
| 섹션 사이 | 32 |

## 컴포넌트 원칙

### 선택지

- **카드 전체가 클릭 영역이다.** 라디오 점만 누르게 하지 않는다
- 최소 높이 **44px** (폰 탭 타겟)
- 선택 상태는 테두리 + 배경으로 표시한다. 체크 표시만으로는 약하다
- **복수정답 문항은 시각적으로 다르게** 보인다 (사각 체크박스형). 필요한 개수를 함께 표시한다 — 123문항이 복수정답이므로 예외가 아니라 일상이다

### 채점 결과

내 선택과 정답을 **동시에** 보여준다.

| 상황 | 표시 |
|---|---|
| 내가 고른 정답 | `correct` 테두리 + ✓ + "정답" |
| 내가 고른 오답 | `wrong` 테두리 + ✕ + "내 선택" |
| 안 고른 정답 | `correct` 테두리 + ✓ + "정답" |
| 안 고른 오답 | 변화 없음 |

아이콘과 텍스트가 항상 색과 함께 간다.

### 해설 블록

순서를 고정한다.

```
① 요구사항/조건   ← surface 배경. 문제를 조건으로 분해한 것이라 먼저 본다
② 정답 해설       ← correct 좌측 보더
③ 오답 해설       ← 아코디언. 내가 고른 오답만 기본 펼침
④ 등장 서비스 칩  ← 누르면 한줄노트가 인라인 확장
```

③을 전부 펼쳐두지 않는 이유: 오답 해설 4개가 한꺼번에 펼쳐지면 화면이 두 배로 길어지고 정작 필요한 하나를 못 찾는다.

### 하단 고정 액션 바

제출·다음 버튼은 화면 하단에 고정한다. 폰에서 긴 지문을 다 스크롤해야 버튼에 닿는 것은 1019번 반복할 동작으로 부적절하다.

`env(safe-area-inset-bottom)`을 반영한다.

## 인터랙션

### 키보드 (PC)

PC에서 1019문항을 푸는 데 실질적 차이를 만든다.

| 키 | 동작 |
|---|---|
| `1`~`5` | 선택지 A~E 토글 |
| `Enter` | 제출 / 다음 |
| `←` `→` | 이전 / 다음 문항 |
| `?` | 단축키 도움말 |

### 모션

- 문항 전환: `opacity` 120ms
- **채점 결과 등장에 애니메이션을 넣지 않는다.** 즉시 표시한다. 1019번 기다리게 하면 안 된다
- `prefers-reduced-motion: reduce`를 존중한다

## 접근성

- 본문 대비 4.5:1 이상
- 탭 타겟 44×44px 이상
- 정오·상태를 색 단독으로 전달하지 않는다
- 포커스 링을 지우지 않는다. 키보드 조작이 1급 입력 수단이다
- 선택지는 시맨틱하게 `role="radio"` / `role="checkbox"`로 노출한다

## 결정하지 않은 것

- 로고·파비콘 — 개인용에 불필요. 필요해지면 그때
- 일러스트·빈 상태 그래픽 — 텍스트로 충분
- 커스텀 폰트 — 위 참고

이 문서에서 가장 바뀌기 쉬운 것은 **색 토큰**이다. 실제 화면을 보고 조정한다. 타이포 스케일과 색 사용 규칙은 바꾸지 않는다.
