# 에이전트 워크플로

> AI 에이전트(Claude Code)를 이 프로젝트 개발에 어떻게 쓰는지에 대한 명세.
> 앱의 기능 명세가 아니라 **개발 과정 자체의 하네스 설계**다.

## 원칙

1. **역할이 아니라 컨텍스트 경계로 분할한다.** PM·프론트·백엔드 에이전트 같은 사람 조직 흉내는 만들지 않는다. 핸드오프마다 정보가 깎이고 토큰만 는다. 서브에이전트는 "메인 세션과 격리된 시선이 필요한가"로만 판단한다.
2. **품질은 프롬프트가 아니라 메커니즘으로 강제한다.** 규칙(CLAUDE.md) → 절차(스킬) → 도구 제한(읽기 전용 리뷰어) 순으로 강한 수단을 쓴다. 판정이 결정적이면 에이전트가 아니라 스크립트가 한다.
3. **그래프는 비용을 상쇄하는 지점에만 쓴다.** 평상시 개발 루프는 메인 세션 하나로 충분하다.

## 구성 요소

### 검증 서브에이전트 (2)

| | 정체 | 역할 | 도구 |
|---|---|---|---|
| 코드 리뷰 | `pr-review-toolkit:code-reviewer` (기존 플러그인 재사용) | 이슈 전체 diff에서 CLAUDE.md 규칙 위반·버그 검사 | 플러그인 기본 |
| 디자인 리뷰 | `.claude/agents/design-reviewer.md` (신규) | DESIGN.md 전용: 토큰 미사용 하드코딩 색상, 색상만으로 정오 표시, 48px 터치 타깃, 타이포 역할 오용 | **읽기 전용** — 고칠 수 없는 리뷰어는 실수로 고치지도 못한다 |

### 스킬 (1)

**`/done`** (`.claude/skills/done/SKILL.md`) — 이슈 종료 절차의 유일한 경로.

1. 이슈의 완료 정의(DoD)를 실제 실행·검증
2. 이슈 전체 diff로 code-reviewer 실행
3. UI 파일(`apps/web` 컴포넌트·스타일) 변경이 포함되면 design-reviewer도 실행
4. 리뷰 지적 사항 반영 (반영 후 재리뷰는 지적이 심각할 때만)
5. 체크박스 patch → 검증 결과 코멘트 → `Done`
6. 외부 상태(CDN·인프라·마이그레이션) 변경 시 MEMORY.md 갱신

### 발동 규칙 (CLAUDE.md에 명문화)

- 이슈 Done 처리는 반드시 `/done` 스킬을 경유한다 — 리뷰는 이슈당 1회, 여기서만 돈다
- CDN publish 전 `pnpm data:verify` 통과 필수

커밋 단위 리뷰는 하지 않는다. 커밋마다 에이전트를 돌리면 커밋을 잘게 쪼갤수록 벌점을 받는 역인센티브가 생긴다. 리뷰 단위 = 이슈(완결된 작업 단위).

### 그래프(Workflow) 투입 지점 (2)

| 지점 | 형태 |
|---|---|
| E2 해부서 판독 | 61페이지 병렬 fan-out 판독 → 스키마 검증 노드 → 병합 |
| E1 추출 교차 검증 | 추출 결과 표본을 독립 에이전트 N개가 원본 PDF와 대조 |

## 도입하지 않는 것

| | 이유 |
|---|---|
| PM·프론트·백엔드 등 역할 에이전트 | PM = Linear 프로토콜, 구현 = 메인 세션 + docs/ 명세가 이미 수행. 분할 시 전화 게임 |
| 커밋 훅(PreToolUse) 강제 | 규칙만으로 게이트가 새는 게 확인되면 그때 승격 |
| QA 에이전트(브라우저 자동화 상시) | 솔로 + 화면 9개. 수동 확인으로 충분. 필요 시 /qa 단발 호출 |
| LangGraph류 프레임워크 | Claude Code Workflow가 실행 그래프 역할을 이미 함 |
